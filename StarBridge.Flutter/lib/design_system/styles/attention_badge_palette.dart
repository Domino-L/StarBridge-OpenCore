import 'package:flutter/material.dart';

/// Accepted unread-count artwork, independent of theme danger/status colors.
abstract final class AttentionBadgePalette {
  static const background = Color(0xFFFF5555);
  static const foreground = Color(0xFFFFFFFF);
}
