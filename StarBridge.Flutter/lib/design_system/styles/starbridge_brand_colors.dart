import 'package:flutter/material.dart';

/// Stable colors sampled from the approved StarBridge application logo.
///
/// These are brand artwork colors, not theme colors: the universal icon must
/// remain identical on light and dark system surfaces.
abstract final class StarBridgeBrandColors {
  static const appIconBackground = Color(0xFF021034);
  static const appIconStar = Color(0xFFFFFFFF);
  static const appIconTrailBlue = Color(0xFF2A70F3);
  static const appIconTrailCyan = Color(0xFF8BDFF6);
  static const appIconTrailMuted = Color(0xFF29456F);
}
